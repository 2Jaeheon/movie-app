name: Validate HTML

on: push

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v2

    - name: Run HTML validation
      run: |
        curl -O https://validator.w3.org/nu/
        java -jar vnu.jar --skip-non-html ./build
